# QuantBot 用户手册

> QuantBot（AI 量化机器人）是一款由大模型驱动的 A 股智能量化机器人。系统采用多 Agent 协作架构（Planner、Quant、CIO、Risk、Trader 五大智能体），覆盖盘前分析、盘中监控、盘后复盘全流程。

- **程序文件**：`QuantBot.exe`
- **技术栈**：Go + React + SQLite + DuckDB
- **适用市场**：中国 A 股（沪深股市）

---

## 1. 安装

本程序为绿色免安装软件，**直接解压到硬盘即可运行**，无需安装程序。

### 1.1 解压与放置

1. 将整个压缩包解压到本地硬盘（例如 `D:\QuantBot\`）。
2. 确保解压后目录结构完整，至少包含以下内容：
   - `QuantBot.exe` —— 主程序
   - `config\config.json` —— 配置文件
   - `data\stock_dict.json` —— 股票代码/名称字典（必需，缺失将导致程序无法正常解析股票）
   - `data\tdx_sector_data.json` —— 板块数据
   - `data\quantpilot.duckdb`、`data\stock.duckdb` —— DuckDB股票时序数据库
   - `database\quantpilot.db` —— SQLite 数据库（投资组合、交易、决策等）
   - `models\` —— XGBoost 机器学习模型文件
   - `log\` —— 运行日志
   - `XtQuant\` —— QMT 实盘交易接口文件（仅实盘模式使用）

### 1.2 启动

双击 `QuantBot.exe` 即可启动。首次启动会自动初始化数据库并进入「欢迎向导」，向导中需要：

1. **设置初始资金**（如 100,000 元）。
2. **配置 AI 服务**（选择 AI 提供商并填写 API Key；也可跳过，稍后在「设置 → AI」中配置）。

启动后进入主界面。左侧为功能菜单，顶部为当前页面。

> 提示：请勿将程序放在会被系统权限限制的目录（如系统盘 Program Files 的受限子目录），避免日志与数据文件写入失败。

---

## 2. 配置

所有配置均在左侧菜单「**设置**」页中完成，分为以下分区：

### 2.1 通用

| 参数           | 说明                     | 建议                  |
| ------------ | ---------------------- | ------------------- |
| 市场           | 固定为 A 股，不可修改           | —                   |
| 语言           | 中文 / English           | 按需选择                |
| 初始资金         | 投资组合的期初本金（影响持仓数量与品种分档） | 最低 10,000，步进 10,000 |
| 主题           | 浅色 / 深色                | 按喜好选择               |
| 实时活动刷新间隔（分钟） | 实时活动、我的投资等页面的自动刷新频率    | 1–60，默认 10          |

### 2.2 交易

- **交易模式**：
  - **模拟接口（推荐）**：使用内置模拟撮合进行交易，不涉及真实资金，用于学习和验证策略。
  - **实盘接口**：通过 QMT（迅投 XtQuant）连接券商进行真实交易。**切换实盘前请务必阅读本节红字风险警告**。
- **QMT 实盘设置**（仅实盘模式可见）：
  - QMT 路径：miniQMT 用户目录，例如 `D:\qmt\userdata_mini`
  - 账号 / 账号类型（STOCK）
  - miniQMT 开关
  - 策略名称 / 策略路径
  - 「测试」按钮：检测 XtQuant 文件夹、Python 环境、XtQuant 库是否就绪

### 2.3 AI

| 参数      | 说明                         |
| ------- | -------------------------- |
| AI 提供商  | DeepSeek / 豆包 / 千问 / Other |
| 模型      | 模型名称，如 `deepseek-Flash`    |
| 接口地址    | 兼容 OpenAI 格式的 Base URL     |
| API Key | 你的模型服务密钥（输入框为密码形式）         |
| 测试连接    | 验证模型可用性，返回模型名、响应时间与回复内容    |

> 保存后可点击「测试连接」验证。系统健康检测中的 AI 服务状态也依赖此处 Key 的有效性。

### 2.4 数据源

实时行情数据源可选，**任选其一并保存**：

| 数据源     | 说明                                    |
| ------- | ------------------------------------- |
| 原生 TDX  | 内置 Go 原生通达信客户端，需先点击「连接」               |
| TDX MCP | 通过 MCP 服务读取通达信数据                      |
| MCP     | 通用 MCP 数据服务                           |
| 通达信终端接口 | 需通达信主程序 `TdxW.exe` 已启动并登录             |
| 腾讯财经    | **无需安装任何软件**，直接读取腾讯财经实时行情，推荐无通达信环境的用户 |

- **TDX 路径**：通达信安装目录，如 `D:\tdx`（用于 tdx_mcp / tdx_terminal）。
- **MCP 配置**：MCP 服务地址（默认 `http://127.0.0.1:8765`）与 API Key。
- 数据源仅影响**实时行情读取**（组合、看板、指数、交易报价等）；研究中心的选股引擎、策略、回测、ETF 监控始终使用本地 DuckDB 数据。
- **财务数据源**（公告/财务同步）：默认**通达信终端**（本地 RPC，不封 IP），也可切换为东方财富；公告数据在巨潮资讯不可用时自动回退到东方财富（单源 6 秒超时）。

### 2.5 其他分区

- **LLM 监控**：查看大模型调用的请求与响应日志。
- **审计日志**：系统全部可审计事件（登录、选股、策略、回测、AI 分析、投资规划、CIO 日志等）。
- **数据维护**：从通达信 `vipdoc` 文件夹中的 `.day` 日线文件同步历史行情到 `stock.duckdb`（会重建 ohlc 行情表，耗时取决于数据量）。在「TDX 数据路径」填入通达信目录后点击「开始同步」。
- **系统健康**：展示数据库、数据源、AI 服务、组合引擎等各组件的初始化与连通状态。
- **关于**：应用信息、版本、核心功能与技术栈，并内置「**软件更新**」：可自动检查新版本、下载更新包并重启应用。

> 所有设置修改后请点击对应分区底部的「**保存**」按钮。

---

## 3. 基本操作

### 3.1 界面总览

左侧菜单栏按功能分组，主要页面包括：

| 菜单       | 功能                                          |
| -------- | ------------------------------------------- |
| 日报 / 仪表盘 | 市场总览、组合状态、当日 AI 决策、持仓快照                     |
| AI 团队    | Planner / Quant / CIO / Risk / Trader 智能体状态 |
| 投资规划     | 基于资金规模的组合规划                                 |
| 投资管理     | 投资规划与执行、每日决策、每日复盘                           |
| 实时活动     | 系统实时事件流（指数、行情、交易、AI 活动）                     |
| CIO          | CIO 决策、市场六维判势、今日交易策略、每日复盘                  |
| 我的投资     | 组合持仓、盈亏、资产曲线                                |
| 选股引擎     | 多因子智能选股（研究中心，含盘口因子）                      |
| 策略       | 内置 17 个策略的管理与指标                             |
| 回测       | 策略历史回测验证                                    |
| ETF 监控   | ETF 池监控                                     |
| 设置       | 系统配置（通用/AI/数据源/审计/数据维护/关于）                 |

### 3.2 每日运行流程

系统按 A 股交易时段自动调度：

1. **盘前（9:15–9:30）**：生成当日 AI 决策（Planner → Quant → Risk → CIO 决策链）。CIO 会先执行**市场六维判势**（技术/广度/量能/资金/情绪/外部 六维实时评分），据此得到**仓位系数**并缩放买入信号（卖出不受影响）；同时读取「今日交易策略」，若策略信号为卖出（如 KDJ 死叉）则生成清仓卖单，且**优先于市场状态机**执行。
2. **盘中（9:30–11:30、13:00–15:00）**：每 10 分钟监控 ACTIVE/RUNNING 投资方案，复用六维判势；若仓位系数过低（<0.2）或判为「退潮风险」则告警并转观望。操盘手按「今日交易策略」信号执行减仓（策略卖出信号优先）。
3. **盘后（15:00 后）**：当日结算（记录总资产、今日盈亏等）、量化因子复盘（15:15，产出各因子质量分并持久化）、策略参数调优（15:05）、CIO 深度复盘与制定明日计划（15:15，依赖因子复盘完成）；盘后自动/量化分析师为**下一交易日选定交易策略**，写入「今日交易策略」。
4. **连续监控**：每日收盘后自动采集股票池**盘口摘要**落库，供次日的盘口因子选股评分使用。

> **市场六维判势（MarketSixDim）**：六维权重为 技术0.15 / 广度0.20 / 量能0.15 / 资金0.15 / 情绪0.25 / 外部0.10。评分经冲突修正后映射到仓位系数——≥80 强势（0.8~1.0）、60~79 结构性震荡（0.4~0.6）、40~59 偏弱（0.2~0.3）、<40 退潮风险（0.0~0.1）。

### 3.3 手动操作

- **运行每日分析**：在日报页点击「运行每日分析」手动触发一次完整 AI 决策链。
- **查看决策**：CIO 页或日报页查看当日决策明细（买入/卖出指令、理由、风险审批）。
- **查看市场判势**：CIO 页「**市场六维判势**」卡片展示最近 7 日的六维得分、仓位系数与市场标签；「**今日交易策略**」卡片展示当前/近期生效的交易策略及已执行卖出次数。
- **启动/停止连续交易**：日报页提供「启动/停止」连续交易开关（模拟模式下用于验证自动撮合）。
- **选股**：在选股引擎页设置筛选条件后运行，结果可加入自选。专业模式下选中个股可在「因子评分」区查看含**盘口**因子（量比/内外盘/封板）在内的各维度得分。
- **检查更新**：在「设置 → 关于 → 软件更新」点击「检查更新」，发现新版本可下载并重启应用。
- **回测**：在回测页选择策略与标的区间运行，查看收益、回撤、夏普、胜率等指标。
- **策略调优**：系统每日自动执行；调优结果自动刷新策略指标，无需重启。

---

## 4. 注意事项

### 4.1 交易规则（A 股）

- **T+1**：当日买入的股票当日不能卖出。
- **交易时段**：9:30–11:30、13:00–15:00；周末及法定节假日休市。
- **整手交易**：买入按 100 股整数倍（自动向下取整）。
- **手续费**：买入收佣金（万三，最低 5 元），不收印花税；卖出收佣金 + 印花税（0.05%）。
- **盘口拦截**：买入会拦截**跌停板**及**涨停封死**（挂单排队买不进）标的；卖出会拦截**跌停封死**（当天无法成交）标的，跌停打开时则允许按跌停价立即离场。
- **盘口五态**：系统按 涨停封死/涨停打开/跌停封死/跌停打开/正常 五态判定盘口状态，并识别「巨量跌停」（量比≥2 且触及跌停）与「内盘占比>0.6 抛压」信号。

### 4.2 数据与持仓口径

- **今日盈亏** = 今日总资产 − 昨日总资产；**累计盈亏** = 今日总资产 − 期初资产（期初资产固定为初始资金）。
- **持仓价值**：交易时段内取实时行情，非交易时段取数据库中最近结算收盘价。
- 页面刷新以数据库数据为准；刷新失败时会弹窗提示并每 30 秒自动重试直至成功。
- 组合会自动过滤高价股（单标的预算不足买入 1 手）并纳入低价股 / ETF；不可交易持仓自动剔除并重新分配权重。
- **持仓数量分档**：<3 万 → 4 只；3–10 万 → 5 只；10–30 万 → 8 只；30–100 万 → 12 只；≥100 万 → 15 只。

### 4.3 设置项注意

- **AI API Key**：请妥善保管，密钥仅保存在本地 `config\config.json`。请勿把该文件分享或提交到公开仓库。
- **实盘模式**：QMT 实盘涉及真实资金，务必先在模拟模式充分验证策略；实盘前确认 XtQuant 环境（Python、XtQuant 库、miniQMT 目录）就绪并通过「测试」。
- **数据源切换**：切换后需点击「保存」并**重新编译/重启程序后生效**（本版本在设置页保存后重启即可生效）。若选「通达信终端接口」，须先启动通达信主程序并登录。
- **指数显示**：指数快照使用实时数据，实时数据获取失败时会弹窗报错，而非显示离线数据。
- **初始资金**：修改初始资金会影响后续持仓分档与建仓规模；CIO 执行买入前会校验真实可用资金，超额时按比例削峰。
- **市场状态**：当市场情绪极弱（confidence ≥ 0.97）时，CIO 不执行常规建仓，仅考虑防御性买入。
- **数据维护**：同步 TDX 日线会重建 ohlc 表，同步期间请勿关闭程序；数据量较大时耗时较长属正常，建议每日在通达信金融终端下载完整的日线数据后再同步数据，确保回测数据准确。
- **日志与备份**：`database\quantpilot.db` 包含全部交易与决策记录，建议定期备份。
- **市场六维判势**：六维数据来自本地 DuckDB 真实行情；其中北向资金以「全市场成交额连续放量天数」代理、炸板率以「日线触板未封」代理，隔夜外围与事件风险取中性默认——这些已在判势卡片标注数据来源，避免误读。
- **策略驱动减仓**：盘后量化分析师会为次日选定交易策略（否则系统按回测指标自动选择）。次日盘前/盘中，若该策略对持仓发出卖出信号（如 KDJ 死叉），操盘手会生成清仓卖单，且**优先于市场状态机执行**。若想改用其它策略，可在盘后让量化分析师通过 `select_next_day_strategy` 工具重新指定。
- **软件更新**：更新包解压后自动校验（禁止覆盖数据库、配置与日志等用户数据），应用下载完成后重启即完成升级。

### 4.4 常见问题

| 现象            | 处理                                  |
| ------------- | ----------------------------------- |
| 启动后停在「系统初始化中」 | 等待后端就绪；长期无响应请查看 `log\` 下日志          |
| AI 分析失败       | 检查设置 → AI 的 Key / 模型 / 地址，用「测试连接」验证 |
| 实时行情获取失败      | 设置 → 数据源 更换数据源（推荐腾讯财经）并测试连接         |
| 指数显示异常或报错     | 实时指数行情获取失败所致，检查网络与数据源               |
| 市场六维判势卡片暂无数据  | 需有 DuckDB 行情且盘前已运行判势；数据不足时显示中性占位并标注来源    |
| 今日交易策略卡片为空    | 盘后量化分析师尚未选定/系统自动选定前为空；盘后自动兜底生成        |
| 选股结果无「盘口」因子    | 盘口因子需 `order_book_daily` 样本累计 ≥20 只才参与评分，且需该标的有收盘盘口数据 |
| 软件更新失败          | 检查网络与更新源；更新包含校验，异常时会提示并回退到重试           |
| 策略回测全部为 0     | 可能为区间数据不足或价格过高；系统已内置动态资金抬升机制缓解      |

---

## 5. 免责声明

1. **本软件仅供学习与研究使用，不构成任何投资建议。**
2. 程序输出的一切信号、评分、决策与报告均基于历史数据与模型运算，**不保证未来收益**，也可能存在错误或遗漏。
3. 金融市场存在风险，使用本软件进行投资所产生的任何盈亏均由使用者自行承担，作者及开发团队不承担任何责任。
4. 实盘交易功能涉及真实资金与真实券商接口，使用前请确认你已理解相关风险，并遵守国家法律法规及交易所、券商的相关规定。
5. 本软件「模拟」模式下的成交、盈亏均为模拟数据，不代表真实市场结果。

---

*QuantBot AI 是一款由大模型驱动的量化机器人。系统采用多 Agent 协作架构（Planner、Quant、CIO、Risk、Trader），覆盖盘前分析、盘中监控、盘后复盘全流程。© 2024-2026 QuantBot lab.*
